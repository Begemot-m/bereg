/**
 * Модуль «Группы и пары» ещё не открыт пользователям. В бою на его месте
 * заглушка «Скоро в PRO»: и карточка в «Инструментах», и сама страница, и
 * роуты — иначе внутрь можно зайти по прямой ссылке.
 *
 * Демо (`NEXT_PUBLIC_DEMO=1`) — витрина, где модуль работает целиком: там его
 * и показываем. Переменной нет — значит бой.
 */
export const GROUPS_LIVE = process.env.NEXT_PUBLIC_DEMO === "1";
