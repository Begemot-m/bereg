# План реализации: главная и рабочие часы

## 1. Главная страница

Файл: `frontend/src/app/page.tsx`

- Сохранить текущие источники данных и роль-зависимое ветвление.
- Заменить повторяющуюся сетку карточек на цветную шапку с ближайшим событием и белый блок рабочего потока.
- Для психолога показать сводку дня, ближайшие записи и действия по клиентам.
- Для клиента показать встречу, быстрый чек-ин и следующий шаг в терапии.
- Для гостя сделать явный сценарий выбора специалиста.
- Свернуть вторичную навигацию и кризисные контакты в компактные элементы.
- Убрать рекламные блоки из центрального потока главной.

## 2. Рабочие часы в «Сессиях»

Файл: `frontend/src/app/sessions/page.tsx`

- Подключить существующий `WorkHoursEditor`.
- Добавить первый показ по локальному флагу `bereg:schedule-setup-seen:v1`.
- При первом входе показать объясняющий блок и раскрытый редактор.
- После сохранения или пропуска показывать компактную сводку графика.
- Открывать редактор повторно кнопкой-шестерёнкой.
- После сохранения обновлять календарь и доступные окна через существующие query invalidations.

## 3. Личный кабинет

Файл: `frontend/src/app/cabinet/page.tsx`

- Удалить редактор и связанную справку.
- Оставить компактный переход в «Сессии», чтобы настройка оставалась обнаружимой.
- Сохранить все параллельные незакоммиченные изменения файла.

## 4. Проверка и публикация

- `bunx tsc --noEmit` или эквивалентный npm-скрипт.
- Production build со статическим экспортом.
- Проверить психолога, клиента и гостя на мобильной ширине.
- Проверить первый и повторный вход в «Сессии», сохранение графика и повторное открытие шестерёнкой.
- Закоммитить только относящиеся к задаче изменения и опубликовать через GitHub Pages.
