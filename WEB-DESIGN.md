# Веб-версия — визуальный язык

Источник правды для того, как «Хроника» выглядит **в браузере**. Правила
мини-приложения — в [DESIGN-RULES.md](DESIGN-RULES.md); они сюда не переносятся.
Растянутая по ширине мобильная вёрстка веб-версией не считается.

Эталон задан владельцем: компактный редакторский дашборд — кремовое полотно,
чёрный сайдбар, пастельные карточки-плитки, структура из цветовых блоков, а не
из рамок и теней.

## Палитра

| Роль | Цвет |
|---|---|
| Полотно страницы | `#F4EEDF` |
| Фон приложения и карточек-основ | `#F6EFDF` |
| Белый (поля, светлые блоки) | `#FFFDF7` |
| Приглушённая карточка (строки списков) | `#EEE8DA` |
| Чёрный (сайдбар, кнопки, текст) | `#111111` |
| Основной текст | `#11110F` |
| Второстепенный текст | `#77736B`, глубже — `#716E66` |
| Волосяная линия | `#D6CEBD` |
| Розовый (главный акцент) | `#ECA4CE`, светлее `#F0B3D4` |
| Жёлтый | `#F3D455` |
| Зелёный | `#9FAF66` |
| Голубой | `#ABC1E6` |

Чёрный держать около `#111111` — не уходить в сине-чёрный. Пастель не заменять
на дежурный SaaS-синий.

## Геометрия

- радиусы: мелкий 8, средний 13, крупный 18, оболочка 24;
- обводка: только волосяная `1px #D6CEBD`, и только там, где без границы не
  читается. Цветные блоки — без рамки совсем;
- **теней нет.** Ни у карточек, ни у кнопок, ни у окон;
- градиентов нет; стекла (blur-панелей) нет;
- отступы плотные: шаг сетки 10, между секциями 17, внутри карточки 16
  (у мелких 14). Воздух не добавлять — интерфейс компактный.

## Типографика

Гротеск (Inter / Manrope / системный). Шкала:

| Роль | Размер | Начертание |
|---|---|---|
| Заголовок экрана | 28 | 650 |
| Заголовок секции | 15 | 600 |
| Заголовок карточки | 14 | 600 |
| Текст | 9–13 | 400 |
| Подписи-капсы | 7–9 | 400, `uppercase`, трекинг |

Иконки — тонкий контур со скруглением. В проекте это Phosphor `regular`/`thin`.

## Компоненты

**Сайдбар.** Чёрный `#111111`, радиус 18, ширина ~147 при полотне 992. Внутри —
знак сверху, дальше группы пунктов с серыми надзаголовками (`General`, `Tools`
→ у нас «Работа», «Инструменты»), внизу выход. Активный пункт: белый текст и
розовая вертикальная полоска 2×18 слева. Ни рамок, ни заливки-подложки.

**Карточки-плитки статистики.** Сплошная пастельная заливка, радиус 15–16,
заголовок вида «Клиенты:», внутри две-три пары «крупное число + капс-подпись»,
справа декоративная фигура на 12–16 % прозрачности и мелкий график (SVG/CSS, без
тяжёлых библиотек).

**Строки списков.** Фон `#EEE8DA`, радиус 14, высота ~39, слева цветная плашка
иконки, справа время.

**Кнопки.** Основная — чёрная пилюля с белым текстом, радиус 14–20. Мягкая —
заливка без рамки. Обводок у кнопок нет.

**Поиск.** Пилюля `#F9F3E7` с волосяной рамкой, слева круглая розовая плашка с
лупой, радиус 14.

## Как это устроено в коде

Слой `[data-web]` в `frontend/src/app/globals.css`. Флаг `data-web="1"` ставится
на `<html>`, когда приложение открыто в браузере (`demo-frame.tsx` в демо,
`phone-shell.tsx` в бою), и переопределяет токены поверх общей разметки:
цвета, радиусы, толщину обводки, тени.

Разметка и данные общие с мини-приложением — расходиться версиям не из чего.
Различаться может структура экрана, но не поведение и не цифры.
