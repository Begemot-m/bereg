# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / frontend
node_modules/
.next/
out/
dist/
.turbo/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Data / logs
*.log
postgres-data/
redis-data/

# Дампы базы. На сервере /opt/bereg — это рабочая копия репозитория, поэтому
# копии рядом со скриптом слишком легко уехали бы в git вместе с данными людей.
deploy/backups/
backups/
*.sql.gz.enc
*.sql.gz

# Скрипт деплоя оставляет их после sed -i.bak и при откате.
deploy/.env.bak
deploy/.previous-image

# Картинки переведены в webp (8.7 МБ -> 1.4 МБ). Исходные png остаются
# локально, но в сборку не едут: на мобильной сети они и были тем, что
# «долго грузилось».
frontend/public/mascots/*.png
frontend/public/mood/*.png
frontend/public/practices/*.png
