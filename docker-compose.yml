services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: psy
      POSTGRES_PASSWORD: psy
      POSTGRES_DB: psy
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U psy"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
