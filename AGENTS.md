# Хроника — контекст проекта для AI-агентов

Прочитай это перед работой. Проект ведётся несколькими AI-инструментами по очереди через git.
Рабочее название приложения — **«Хроника»** (бренд в `frontend/src/lib/brand.ts`). `bereg` — историческое имя репозитория, а ключи `bereg_*` в localStorage остались от прошлых названий: их не переименовываем, это сломает деплой и данные пользователей. Домен прода — `chronika.space`, и он единственный: других адресов у приложения нет, в ссылках и конфигах их быть не должно.

## Старт в новом чате

Владелец работает с нескольких компьютеров, в Claude Code и в Codex. Единственный источник
правды — git, не память инструмента. Порядок:

1. `git pull --ff-only` — **до** любых правок;
2. `git status` — посмотреть, не осталось ли незакоммиченного от прошлого инструмента;
3. прочитать этот файл целиком (он короткий) — раздел «ТЕКУЩАЯ ЗАДАЧА» ниже говорит, чем заняты сейчас;
4. **дальше читать только тот файл, который нужен под конкретную задачу**, а не все подряд:

   | Файл | Когда открывать |
   |---|---|
   | `CLAUDE.md` | только Claude Code — его локальные правила работы |
   | `PERFORMANCE-HANDOFF.md` | задача про скорость/аудит производительности |
   | `HANDOFF.md` | нужно понять, что уже сделано и что дальше по фичам |
   | `FEATURE-FLOW.md` | добавляем фичу с данными (демо-мок + боевой роут) |
   | `DESIGN-RULES.md` | правила стилей от владельца — первоисточник, короткий |
   | `DESIGN.md` | трогаем UI |
   | `RELEASE.md` / `UPDATES.md` | деплой, миграции, выкатка |
   | `BACKEND.md` | Prisma, API-роуты, база |
   | `CHANGELOG.md` | **только по grep.** Журнал всех прошлых правок — 2400 строк. Целиком не открывать никогда: ищите там ответ на «почему это сделано так», а не «что делать» |

   Открыть все семь «на всякий случай» — это ~90 КБ в контексте до первой строчки кода. Не надо.

   **Сменили компьютер** — до первого `bun run check` сделайте
   `cd frontend && bunx prisma generate`. Сгенерированный клиент лежит в
   `node_modules` и через git не едет: без этого типы падают два десятка раз
   на «Property 'group' does not exist on PrismaClient», и это не ошибки кода.

5. закончил правки — **одна команда, без вопросов и обсуждений**:

   ```bash
   cd frontend && bun run ship "что сделали"
   ```

   Она сама прогоняет `check`, коммитит, пушит, запускает сборку демо и падает,
   если что-то не сошлось. Спрашивать «коммитить ли?» не нужно: это часть
   задачи, а не отдельное решение. Незапушенная работа = следующий инструмент
   начнёт с устаревшей базы, а демо останется на старом коммите.
   Проверить, что доехало: `bun run deploy:status`.

   У Claude Code на это стоит страж: хук `Stop` (`.claude/hooks/unshipped.mjs`)
   не даёт закончить сессию, пока в `master` есть незакоммиченное или
   незапушенное.

6. **записать сделанное — коротко.** В этот файл идут 5–10 строк: что
   изменилось, в каких файлах, что осталось. Подробный рассказ — в
   `CHANGELOG.md`, сверху, под своей датой. Правило появилось 19 августа:
   AGENTS.md дорос до 1300 строк (~42 000 токенов), и каждая сессия вливала их
   в контекст до первой строчки кода, а контекст пересылается заново на каждый
   вызов инструмента. Держим файл в пределах ~200 строк.

## ТЕКУЩАЯ ЗАДАЧА — 20 августа 2026

**Приглашение своих клиентов ссылкой, в демо.** Специалист зовёт клиента двумя
ссылками, обе ведут в мини-приложение:

- афиша свободных окон — `win_<id>` в стартовой метке бота, экран
  `/windows?psy=<id>`. Вертикальная карточка: портрет, имя, метод, свободное
  время на неделю или месяц. Тап по окну открывает анкету с записью;
- визитка — прежняя `book_<id>` (`bookingInviteUrl`), она уже вела в каталог с
  открытой записью.

Отдельного экрана больше нет: 20 августа `/cabinet/invite` удалён, всё живёт во
всплывающем окне `ClientInviteSheet` (`components/client-invite.tsx`). Сверху —
миниатюра анкеты и её ссылка, ниже два пути: «Отправить приглашение с
расписанием» (готовый текст со свободными окнами, `inviteMessage` в
`lib/invite-windows.ts`) и «Сформировать расписание картинкой» (афиша
1080×1920, `posterPng` в `components/invite-poster.tsx`). Открывается из
баннера `ClientInviteBanner` на главной и строкой «Пригласить клиента» в
кабинете.

Под именем в кабинете вместо «Редактировать профиль» — «Моя анкета в каталоге»
(ведёт в `/catalog?psy=…&from=cabinet`); у пустой анкеты кнопка честно зовёт её
заполнить.

Окна для афиши считает `lib/invite-windows.ts` поверх боевых `/month-availability`
и `/slots` — новых роутов фича не потребовала, в демо всё работает на моках.

---

## ПРОШЛАЯ ЗАДАЧА — 19 августа 2026

**Модуль «Группы и пары», работаем только в демо.** Прод трогать не нужно:
заглушка «Скоро в PRO» там уже стоит, `GROUPS_LIVE` в `lib/modules.ts`
открывает модуль только при `NEXT_PUBLIC_DEMO=1`. Значит правки едут на демо
(`bun run ship` собирает его сам), выкатка на VPS в эту задачу не входит.

Что уже работает — раздел ниже. Что доделываем — список «Дальше» в конце
раздела. Прошлые пакеты владельца (17–19 августа) закрыты, их описания — в
`CHANGELOG.md`.

### Каталог грузился минутами — фото уехали из JSON — 20 августа

Владелец: «каталог психологов очень долго открывается». Причина: снимки лежат
data-URL'ом в `PsyProfile.data`, и `/api/catalog` отдавал их прямо в списке —
до мегабайта base64 на карточку, без кеша и без ленивой загрузки, плюс те же
сотни мегабайт поднимались из базы в память сервера.

- новый роут `api/catalog/photo/[psy]/[i]` отдаёт снимок файлом с вечным
  кешем; версия анкеты в `?v=` меняет адрес, когда фото заменили;
- `lib/server/psy-card.ts` кладёт в карточку ссылки вместо base64
  (`psyPhotoUrls`), а `psyProfileRows` читает анкеты запросом без `photos`;
- `catalog/page.tsx` грузит вперёд только два верхних портрета, остальные —
  лениво, и `next/image` наконец ужимает их под плашку.

**На проде сейчас `sha-d45484a`** (20 августа): починен просмотр фотографий в
анкете — область свайпа больше не пересоздаётся на смене кадра, поэтому после
листания и закрытия экран не глохнет. Подробности — в CHANGELOG.

Перед этим на прод уезжал `sha-5ee8f89` (вместе с ним — настоящие
кадры интерфейса на лендинге, `1540e95`). Живая база новые SQL-запросы
(`data - 'photos'`, `jsonb_array_length`) приняла: `/api/catalog` отвечает 200
за ~0,9 с и весит 181 КБ вместо мегабайтов, base64 в ответе не осталось, фото
идут ссылками `/api/catalog/photo/<psy>/<i>?v=…` — `image/jpeg` с
`cache-control: public, max-age=31536000, immutable`. Модуль групп в бою как
был закрыт: `NEXT_PUBLIC_DEMO` на сервере не задан, значит `GROUPS_LIVE=false`
и на `/groups` стоит заглушка «Скоро в PRO».

### Модуль PRO «Группы и пары» — 19 августа (витрина + рабочий дашборд)

Первый платный модуль для специалиста: парная терапия и ведение групп.

**Где живут платные модули.** Вкладка «Инструменты» у специалиста: сначала
чёрный баннер «Скоро в Хронике», под ним блок «Модули», дальше практики. Новый
таб не заводим (пять — потолок нижней навигации), кабинет не годится: туда
заходят раз в месяц за оплатой. Сами модули — на своих роутах верхнего уровня
(`/groups`), чтобы ссылка из календаря была короткой.

**Витрина.** `components/pro-modules.tsx`: карточка того же вида, что практики
ниже (фоновая иллюстрация, название, описание, кнопка перехода) плюс плашка PRO
в углу. Иллюстрация нарисована вёрсткой — круги участников вокруг ведущего в
тоне раздела, картинки для групп в `public` нет. Анонсов будущих модулей на
витрине нет: показываем только то, что работает. Без PRO карточка не мёртвая —
тап открывает `ProPaywall`, на самой странице модуля стоит `ModuleLocked`.

**Данные (обе стороны, как требует FEATURE-FLOW).**
- `prisma/schema.prisma` — модели `Group` и `GroupMember`, миграция
  `20260819130000_groups` (две новые таблицы, аддитивно);
- `lib/groups.ts` — клиент: список, создание, правка, состав;
- `lib/server/groups.ts` — `MEMBERS_INCLUDE` и `NEEDS_PRO_MODULE`. Лежат вне
  `app/api` намеренно: из `route.ts` Next разрешает экспортировать только
  обработчики, и `bunx tsc --noEmit` этого не ловит — падает только сборка;
- `app/api/groups/**` — GET/POST списка, GET/PATCH/DELETE группы, POST/DELETE
  состава. Везде `access(user.id).pro`, иначе 402: запирать модуль только в
  интерфейсе нельзя. Владение проверяется по токену, чужие `clientId` из тела
  отсеиваются;
- `lib/demo.ts` — те же роуты в моке, тип `Group` в `DB`, демо-группа
  «Опоры» с двумя участниками, чтобы дашборд не встречал пустотой.

**Данные встреч.** Модели `GroupMeeting` и `GroupAttendance`, миграция
`20260819160000_group_meetings`. Роут `app/api/groups/[id]/meetings` — POST
(одна встреча или цикл: `repeatWeeks` повторов по той же неделе), PATCH
(статус и отметки посещаемости, чужие `memberId` отсеиваются), DELETE. Те же
ветки в моке, демо-группа идёт циклом из шести встреч по вторникам: две прошли
и отмечены.

**Экраны.** `/groups` — дашборд: жёлтая строка «Отметьте, кто был» для
прошедшей неотмеченной встречи, крупная карточка ближайшей встречи с датой,
составом и номером сессии в цикле, ниже список групп (лица, места, следующая
дата). Плиток со счётчиками нет намеренно: ведущему нужно «что ближайшее» и
«что требует внимания», а не «групп: 1».

`/groups/[id]` — карточка: сводка (места + следующая встреча), расписание
(будущие сверху, прошедшие ниже с «пришли 6 из 8»), состав с посещаемостью по
каждому и ярлыком «пропадает» после трёх пропусков подряд. Действия разделов —
текстом в заголовке, как «График» и «Календарь» в «Сессиях»; редкое
(переименовать, мест, удалить) убрано под раскрытие «Настройки группы».
Отметка посещаемости: все пришли по умолчанию, ведущий снимает отсутствующих.
Всё разрушающее — через `useConfirmAsk`.

**Встречи группы в общем календаре — сделано 19 августа.** Окно занято группой
целиком: стопка лиц, название вместо имени клиента, тон модуля (salmon), тап
ведёт на `/groups/[id]` — раскрывать плитку нечем, состав и посещаемость живут
в карточке. Прошедшая неотмеченная встреча помечена янтарными часами, как
строка «Отметьте, кто был» на дашборде.
- `week-windows.tsx` — тип `GroupSlot`, встречи подмешиваются в `useDayWindows`
  (запрос групп только при `GROUPS_LIVE`), плитку рисует `GroupCell`;
- `lib/server/schedule.ts` — новый `groupBusy`, он идёт в `checkSlotOpen` и
  `takenTimes`: клиент из каталога не увидит это время свободным и не запишется
  в него ни новой записью, ни переносом;
- `lib/demo.ts` — тот же `groupBusy` в `busyOf` и `slotOpen`.

**Лента, перенос и динамика — сделано 19 августа (вечер).**
- **404 вылечен:** карточка открывается как `/groups/?id=N` (страница списка
  сама рендерит `GroupDetail`), потому что статика знает только `/groups/901`.
  Ссылки на группу ставить только в этом виде. Старым демо-базам досевается
  демо-группа в `load()`;
- **лента группы** — модель `GroupPost` + миграция `20260819210000_group_posts`,
  роут `groups/[id]/posts`. `announce()` в `lib/server/groups.ts` и в `demo.ts`:
  одно изменение = одно сообщение всем участникам (перенос, отмена, новый цикл,
  задание, участник, место/формат/описание). Посещаемость молчит намеренно;
- **перенос встречи** — `PATCH .../meetings` со `startsAt`, кнопка в строке
  встречи, лист `MoveForm` с охватом «уйдёт N участникам»;
- **динамика состояний** — роут `groups/[id]/mood`, вкладка «Состав»: средняя
  линия группы + линия по каждому (`MoodTrend`/`Spark` в `groups-ui.tsx`);
- плитка группы в календаре ужата до размера обычного окна.

**Ссылка на набор — сделано 19 августа (вечер).** Переход по ней сам заводит
карточку клиента у ведущего, цепляет терапевта и ставит человека в состав:
заявок на вход нет намеренно — ссылку дают тем, кого уже позвали.
- метка `grp_<id>-<подпись>`, тот же короткий код, что у `psy`/`inv`
  (`lib/server/invite-code.ts`, `lib/invite.ts`);
- `GET /api/groups/[id]/invite` — токен ведущему (PRO + владение),
  `POST /api/groups/join` — приём (лимит, `psyApproved`, PRO ведущего, места,
  своя ссылка → `self`). Те же ветки в `lib/demo.ts`, в демо метка — `g<id>`;
- `/api/invite/preview` отдаёт `group` — экран «вас приглашают в группу «X»»
  (`invite-welcome.tsx`);
- у ведущего блок «Ссылка на набор» во вкладке «Состав» — пока есть места;
- на сайт ссылка приходит и как `?group=<токен>`.

### Сайт chronika.space — 20 августа (пакет правок владельца)

Лендинг — `components/web-landing.tsx`, нарисованные экраны разделов —
`components/landing-screens.tsx`, вопросы — `lib/seo.ts` (там же микроразметка
FAQPage: текст правится только в одном месте). Сделано: новое УТП в герое
(вторая строка лавандовым), блок «С чего начать» из пяти шагов (`#start`),
тона вкладок сведены с `accentFor` в `app-shell.tsx`, экран «Инструменты»
перерисован под настоящий раздел, вход из шапки и подвала убран (веб-версии
пока нет), реквизиты переехали в `/docs`. Гость на десктопе больше не видит
лендинг вместо документов: `isPublicDoc()` в `app-shell.tsx` пускает `/docs/**`
и `/policy` без входа. Подробности — в `CHANGELOG.md` за 20 августа.
Выкачено на прод: `sha-594907c`.

**Картинки интерфейса на сайте — настоящие кадры, не рисунки (20 августа,
вечер).** Рисованные копии разделов расходились с приложением, поэтому лендинг
показывает снимки живого демо из `frontend/public/shots`: `w-*.webp` — широкий
кадр с сайдбаром для блока вкладок, `p-*.webp` — телефон целиком для карточек
«Возможности». Пересъёмка: `bun run demo`, затем `node scripts/landing-shots.mjs`
и `node scripts/landing-sheet.mjs` (контактный лист для проверки). Скрипт сам
готовит демо-базу, гасит обучающие колоды и подделывает Telegram-десктоп, чтобы
демо не надело рамку телефона. `landing-screens.tsx` остался ради `psy-guide.tsx`.

**Дальше:**
1. Сводка динамики на дашборде `/groups` — сейчас она живёт только в карточке
   группы.
2. Что видит по группе сам участник: расписание и лента у клиента.
3. Чек-ины до и после встречи и парный режим: общие заметки на пару и
   раздельные приватные по каждому.

**Проверять правки статическим экспортом.** `bunx tsc --noEmit` пропускает
ошибки роутов и `generateStaticParams`. Локально это ловится так:
`mv src/app/api ../.api-tmp && STATIC_EXPORT=1 NEXT_PUBLIC_DEMO=1 bunx next build; mv ../.api-tmp src/app/api`
— ровно то, что делает CI.

## Что это
«Хроника» — платформа для психологов (Telegram Mini App + веб). Три роли: **гость / клиент / психолог**. Психолог ведёт клиентов, записи, задаёт рабочие часы; клиент записывается в свободные окна. Центр-создатель — «Амур и Психея» (упомянут в кабинете). Живой прототип: https://begemot-m.github.io/bereg/

## Стек
- **Next.js 15 (App Router) + TypeScript**, фронт и бэк в одном проекте (`frontend/`).
- **Tailwind v4**, **motion** (framer-motion) для анимаций, **@phosphor-icons/react** для иконок.
- **Prisma + PostgreSQL** и API-роуты (`src/app/api`) — для боевого режима. НЕ используются в демо.
- **Демо-режим** (`NEXT_PUBLIC_DEMO=1`): всё работает в браузере на моках в `localStorage`, без бэкенда. Именно он задеплоен.

## Как запускать (папка frontend/)
```bash
bun install
bun run demo        # демо на моках → http://localhost:3000  (для разработки/просмотра)
bun run build       # прод-сборка
bunx tsc --noEmit   # проверка типов
```

## Деплой (важно)
Пуш в ветку `master` на GitHub (`Begemot-m/bereg`) → GitHub Actions собирает **статический экспорт** демо и публикует на GitHub Pages. Настроено в `.github/workflows/deploy.yml`.
- Статическая сборка: `STATIC_EXPORT=1 NEXT_PUBLIC_DEMO=1 PAGES_BASE_PATH=bereg`, CI удаляет `src/app/api` (в статике не нужен).
- `next.config.mjs` включает `output: "export"` и `basePath` только когда `STATIC_EXPORT=1`.
- Значит: **любая правка → git push → через ~2 мин на сайте**. Ломать статический экспорт нельзя (нет серверных `redirect()` в страницах, динамические роуты требуют `generateStaticParams`).
- **Пуш не считается выкаткой.** Бывало, что пуш не рождал сборку вовсе (зависшая очередь Pages), и демо неделю жило на старом коммите при зелёном локальном чеке. Поэтому вместо голого `git push`:
  ```bash
  cd frontend && bun run ship "что сделали"   # check + commit + push + сборка демо
  ```
  Внутри — `deploy:demo`: он пушит, явно запускает workflow (push-триггер в этом репозитории молчит) и двигает тег `demo-live` на выкаченный коммит. Молча «выкачено» больше не бывает.
- Проверить руками, что сейчас в демо: `curl -s https://begemot-m.github.io/bereg/version.json` — там `build` и `sha` выложенной сборки.
- Если сборка не появляется или висит в очереди — первым делом смотри https://www.githubstatus.com: при авариях Actions/Pages пуши перестают создавать сборки вообще. Скрипт такую аварию распознаёт и говорит прямо.

## Структура (frontend/src)
- `lib/demo.ts` — мок-бэкенд (перехват `apiFetch`), все данные и правила демо здесь.
- `lib/api.ts` — в демо шлёт в `mockFetch`, иначе на `/api`.
- `lib/role.ts`, `lib/profile.ts` — роль и профиль психолога (localStorage), онбординг-флаг.
- `lib/schedule.ts` / `components/work-hours.tsx` / `components/slot-picker.tsx` — рабочие часы психолога и запись в свободные окна.
- `lib/haptics.ts` — микровибрации (Telegram + navigator.vibrate).
- `components/app-shell.tsx` — навигация (сайдбар/табы), гейт онбординга.
- `components/onboarding.tsx` — знакомство: роль → УТП под роль → анкета психолога.
- `components/ui.tsx`, `components/blocks.tsx` — примитивы (Button/Card/Disclosure/StatTile/ModuleCard).
- `app/page.tsx` (главная, роле-зависимая), `app/sessions`, `app/clients`, `app/cabinet`, `app/catalog`, `app/tools`.
- `components/demo-frame.tsx` — рамка-телефон вокруг демо на десктопе.

## Дизайн-конвенции
- Светлая система: бумага `#f9f8f3`, графит `#2c2e31`, акценты ирис `#5b5bd6` / шалфей `#2e7d63`. Токены в `app/globals.css`.
- **Сплошные заливки** блоков (`.fill-iris/.fill-sage/.fill-ink`), минимум визуального шума, единый радиус (`--r-block`).
- Доп. опции — раскрывать через `<Disclosure>`, не наваливать на экран.
- Иконки только из Phosphor. Без эмодзи в UI.

## Текущее состояние / что дальше (примерно)
Готово: онбординг, роли, сессии со слотами, рабочие часы по конкретным часам недели, карточка клиента (статы/настроение/домашки), напоминания, каталог с записью в окна, расширенный профиль психолога (тэги-запросы/образование/подход/о себе/фото из ТГ + загрузка), подписка (ЮKassa-заглушка), отдел заботы.
Дальше по желанию владельца: наполнение раздела «Инструменты», реальный бэкенд (Prisma/Postgres, ЮKassa-боевой, бот напоминаний), связка каталога с реальными психологами.

## Массовые рассылки из бота
Разовый анонс всем пользователям (`sendPhoto`/`sendMessage` по списку из базы) —
операция необратимая: отправленное не отзывается, а повторный запуск дублирует
сообщение живым людям. Поэтому скрипт рассылки обязан:

- **вести журнал отправок** — дописывать `chat_id` в файл сразу после успеха, с
  `buffering=1`. На старте читать журнал и выкидывать из очереди тех, кто в нём
  уже есть. Без этого обрыв сети посреди прогона оставляет серую зону: кому
  дошло, а кому нет, узнать нельзя — Telegram истории отправок не отдаёт;
- **переживать сетевые сбои** — 3–4 попытки с нарастающей паузой на таймауты и
  разрывы TLS, а не падать с трейсбеком (18 августа рассылка так оборвалась на
  49-м из 113 адресатов);
- **кэшировать `file_id` картинки** — первому получателю фото уходит файлом, из
  ответа берётся `file_id`, дальше в запросе идёт он. Иначе каждый адресат
  заново тянет мегабайты;
- **держать темп** ~20 сообщений в секунду (лимит Telegram около 30) и молча
  проглатывать `403` (заблокировал бота) и `chat not found` (никогда не писал
  боту) — на сотне адресатов это норма, а не ошибка;
- **начинать с одного получателя** — своего chat_id, чтобы увидеть вёрстку
  поста до того, как он уйдёт всем.

Роль психолога в базе называется `psychologist` (не `psy` — так зовётся только
префикс кодов приглашений). Получателей брать через
`roles: { has: "psychologist" }`.

## Правила
- Отвечать на русском. Комментарии к очевидному коду не добавлять.
- Перед началом — `git pull`; после — `git commit` + `git push` (иначе рассинхрон между инструментами).
- Демо не должно требовать сервера: новые данные — через `lib/demo.ts` (мок), не через реальные `/api` вызовы в демо-режиме.
